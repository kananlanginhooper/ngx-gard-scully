# NgxGardSkully
Run `npm install`

# Development
Run `ng serve`

# Rendering Production
Run `npm run prepare`
Run `npm run scully -- --scanRoutes`

# Running production server
Run `npm run scully:serve`